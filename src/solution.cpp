"Cafe"라는 클래스를 만드세요. 이 클래스는 "coffeeType", "price" 그리고 "quantity"라는 세 개의 private 멤버변수를 가지고 있습니다. Cafe 클래스는 다음과 같은 메소드를 가져야합니다:

- 기본 생성자: 모든 멤버변수에 0 또는 빈 문자열을 설정합니다.
- 매개변수를 갖는 생성자: 모든 변수에 값을 할당합니다.
- "setCoffee" 함수: coffeeType에 값을 설정합니다.
- "setPrice": price에 값을 설정합니다.
- "setQuantity": quantity에 값을 설정합니다.
- "getTotalPrice": total price를 반환하는 함수. 이는 price * quantity로 계산됩니다.