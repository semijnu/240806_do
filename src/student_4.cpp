#include <iostream>
#include <string>
using namespace std;

bool isPalindrome(string str) {
    // 여기에 코드를 작성하세요.
}

int main() {
    string input;
    getline(cin, input);
    cout << (isPalindrome(input) ? "true" : "false") << endl;  
    return 0;
}