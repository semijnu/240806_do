#include <iostream>
using namespace std;

int maxOfThree(int a, int b, int c) {
    // Write your code here.
}

int main() {
    int num1, num2, num3;
    cin >> num1 >> num2 >> num3;
    cout << maxOfThree(num1, num2, num3) << endl;  // Removes the prompt message to simplify the input and output process.
    return 0;
}