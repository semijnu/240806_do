#include <iostream>
using namespace std;

int sum(int a, int b) {
    // Write your code here.
}

int main() {
    int num1, num2;
    cin >> num1 >> num2;
    cout << sum(num1, num2) << endl;  // Remove the prompt message to simplify inputs and outputs.
    return 0;
}