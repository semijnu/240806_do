#include <iostream>
#include <stack>
#include <string>

using namespace std;

bool is_balanced(string input) {
    // 여기에 코드를 작성하세요.
}

int main() {
    string input;
    getline(cin, input);
    cout << (is_balanced(input) ? "YES" : "NO") << endl;
    return 0;
}